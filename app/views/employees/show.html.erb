<div class="container">
    <h1>Employee No.<%= @employee.id %></h1>
    <table>
        <tr>
            <th>First Name</th>
            <th>Middle Name</th>
            <th>Last Name</th>
            <th>Role</th>
            <th>Salary</th>
        </tr>
        <tr>
            <td><%= @employee.first_name %></td>
            <td><%= @employee.middle_name %></td>
            <td><%= @employee.last_name %></td>
            <td><%= @employee.role %></td>
            <td><%= @employee.salary %></td>
        </tr>
    </table>
    <%= link_to "Back", show_all_employees_path%>
</div>